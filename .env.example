# Server Configuration
PORT=5000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/vox-edge-media

# Firebase Admin SDK Configuration (Choose one option)

# Option 1: Use service account file (Recommended for development)
# Download from: Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Save as: config/serviceAccountKey.json
# No environment variables needed if using this option

# Option 2: Use environment variables (Recommended for production)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"

# Note: For FIREBASE_PRIVATE_KEY, keep the quotes and include \n for line breaks

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
FROM_EMAIL=your-email@gmail.com
FROM_NAME="Vox Edge Media"
